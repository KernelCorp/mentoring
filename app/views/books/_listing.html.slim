table.ui.table.celled
  thead
    tr
      th Название книги
      th Опубликовал

  tbody
    - if books.blank?
      tr
        td.disabled colspan=2
          | Нет данных для отображения
    - else
      - books.each do |book|
        tr
          td = link_to book.name, book_path(book), class: 'block'
          td = book.owner.email
