<h2 class="ui header breadcrumb">
  <%= link_to t('forem.forum.forums'), forem.forums_path, class: "section" %>
  <div class="divider"> / </div>
  <%= link_to forem_emojify(topic.forum.title), [forem, topic.forum], class: "section" %>
  <div class="divider"> / </div>
  <div class="active section"><%= forem_emojify(topic.subject) %></div>
</h2>