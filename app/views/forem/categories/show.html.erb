<div class='category'>
  <h2 class="ui header breadcrumb">
    <%= link_to t('forem.forum.forums'), forem.forums_path, class: "section" %>
    <div class="divider"> / </div>
    <div class="active section"><%= forem_emojify(@category) %></div>
  </h2>
  <%= render @category %>
</div>